import 'package:flutter/material.dart';

class SpeedMentoringScreen extends StatelessWidget {
  const SpeedMentoringScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Speed Mentoring')),
      body: const Center(child: Text('Speed Mentoring - Coming Soon')),
    );
  }
}
