import 'package:flutter/material.dart';

class TodaysMatchesScreen extends StatelessWidget {
  const TodaysMatchesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Today's Matches")),
      body: const Center(child: Text("Today's Matches - Coming Soon")),
    );
  }
}
