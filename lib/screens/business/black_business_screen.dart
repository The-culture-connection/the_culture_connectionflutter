import 'package:flutter/material.dart';

class BlackBusinessScreen extends StatelessWidget {
  const BlackBusinessScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Black-Owned Businesses')),
      body: const Center(child: Text('Black-Owned Businesses - Coming Soon')),
    );
  }
}
